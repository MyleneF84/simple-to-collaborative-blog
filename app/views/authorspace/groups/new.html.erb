<%= simple_form_for ([:authorspace, @author, @group]) do |f| %>
  <%= f.input :name %>
  <%= f.simple_fields_for :memberships do |p| %>
    <%= p.input :author_id, collection: Author.all, label_method: :full_name %>
    <%#= p.collection_check_boxes :author_id, Author.all, :id, :full_name %>
    <%#= p.collection_select :author_id, Author.all, :id, :full_name %>
    <% end %>
  <%= f.submit "Create your new group", class: "btn btn-primary" %>
<% end %>
