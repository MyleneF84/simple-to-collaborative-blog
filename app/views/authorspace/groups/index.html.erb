<h1>My groups</h1>

<div class="row mb-4">
  <% @groups.each do |group| %>
    <div class="col-6">
      <%= link_to "#{group.name}", authorspace_author_group_path(current_author, group) %> - <%= group.authors.map(&:full_name).join(", ") %>
      <%= link_to "🗑", authorspace_author_group_path(current_author, group), data: {turbo_method: :delete, turbo_confirm: "Delete #{group.name} ?"} if policy(group).destroy?  %>
    </div>
  <% end %>
</div>

<%= link_to "Add a group", new_authorspace_author_group_path, class:"btn btn-primary"  %>
