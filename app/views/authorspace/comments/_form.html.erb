<%= simple_form_for (@article ? [:authorspace, @article, @comment] : [:authorspace, @author, @comment]) do |f| %>
  <%= f.input :content, required: 'required', label: "Your comment :"  %>
  <%= f.submit  %>
<% end %>
